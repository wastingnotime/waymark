# Simulation Slice Documents

Each slice document is a bounded hypothesis for one vertical simulation
increment. It records the selected implementation pack, runtime target,
discovery scope, use-case contract, rules, ports, deterministic test plan,
scenario, done criteria, and out-of-scope behavior.

Building a slice may split, merge, reorder, or remove candidate slices. Update
the semantic model and this directory when implementation evidence changes the
hypothesis.
